import MUIDeleteIcon from "@mui/icons-material/DeleteOutlined";
import red from "@mui/material/colors/red";

const DeleteIcon = () => {
  return <MUIDeleteIcon sx={{ color: red[500] }} />;
};

export default DeleteIcon;
