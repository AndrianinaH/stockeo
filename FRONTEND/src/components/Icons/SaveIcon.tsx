import MUICheckIcon from "@mui/icons-material/Check";
import green from "@mui/material/colors/green";

const SaveIcon = () => {
  return <MUICheckIcon sx={{ color: green[500] }} />;
};

export default SaveIcon;
