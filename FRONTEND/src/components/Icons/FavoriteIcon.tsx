import MUIStarIcon from "@mui/icons-material/Star";
import amber from "@mui/material/colors/amber";

const FavoriteIcon = () => {
  return <MUIStarIcon sx={{ color: amber[500] }} />;
};

export default FavoriteIcon;
